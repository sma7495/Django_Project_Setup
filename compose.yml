# Additional recommendations:

#     Consider creating a .env file at the root for common environment variables

#     Add a networks section if you need custom network configuration

#     For production, remove the ports for internal services (like Redis and DB)

#     Consider adding resource limits (CPU/memory) for production

#     For Django, consider using Gunicorn instead of runserver in production

#     Add a frontend service if you have a web client

#     Consider using Docker profiles for different environments (dev/prod)


services:
  # Cache Layer............

  # redis:
  #   image: redis:7-alpine
  #   container_name: crm_redis
  #   restart: unless-stopped
  #   command: redis-server --save 60 1 --loglevel warning
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data

  #   # Health checks: Added health checks for Redis or PostgreSQL to ensure services are ready
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3

  # Database....................
  db:
    image: postgres:17-alpine
    container_name: crm_db
    restart: unless-stopped
    environment:
    # Environment variables: Added defaults using ${VARIABLE:-default} syntax
      POSTGRES_USER: ${POSTGRES_USER:-myuser}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mypassword}
      POSTGRES_DB: ${POSTGRES_DB:-mydatabase}
      # - POSTGRES_USER=myuser
      # - POSTGRES_PASSWORD=mypassword
      # - POSTGRES_DB=mydatabase
    ports:
      - "5432:5432"

    # named Volume...........
    volumes:
      - postgres_data:/var/lib/postgresql/data
    
    # bined Volume...........
    # volumes:
    #   - ./postgres/data:/var/lib/postgresql/data
      
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-myuser}"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Application Backend
  backend:
    build:
      context: .
      dockerfile: ./dockerfiles/dev/django/Dockerfile
    container_name: crm_backend
    working_dir: /usr/src/app
    command: python manage.py runserver 0.0.0.0:8000
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./core:/usr/src/app
    env_file:
      - ./envs/dev/django/.env

    depends_on:
      db:
        condition: service_healthy
      # redis:
      #   condition: service_healthy

    dns:
      - 8.8.8.8
      - 8.8.4.4

  # Celery Worker.......................................

  # worker:
  #   build:
  #     context: .
  #     dockerfile: ./dockerfiles/dev/django/Dockerfile
  #   container_name: crm_celery_worker
  #   working_dir: /usr/src/app
  #   command: celery -A core worker --loglevel=info
  #   # command: celery -A core worker --beat --scheduler django --loglevel=info
  #   restart: unless-stopped
  #   volumes:
  #     - ./core:/usr/src/app
  #   env_file:
  #     - ./envs/dev/django/.env
  #   depends_on:
  #     - redis
  #     - backend


  # Celery Beat Scheduler..........................

  # beat:
  #   build:
  #     context: .
  #     dockerfile: ./dockerfiles/dev/django/Dockerfile
  #   container_name: crm_beat
  #   working_dir: /usr/src/app
  #   command: celery -A core beat -l info
  #   restart: unless-stopped
  #   volumes:
  #     - ./core:/usr/src/app
  #   env_file:
  #     - ./envs/dev/django/.env
  #   depends_on:
  #     - redis
  #     - backend
  #     - worker

  # Development SMTP Server....................

  # smtp:
  #   image: rnwood/smtp4dev:latest
  #   container_name: crm_smtp
  #   restart: unless-stopped
  #   ports:
  #     - "5000:80"    # Web UI
  #     - "1025:25"    # SMTP (changed from 25 to avoid permission issues)
  #     - "1143:143"   # IMAP (changed from 143 to avoid conflicts)
  #   volumes:
  #     - smtp_data:/smtp4dev
  #   environment:
  #     ServerOptions__HostName: smtp4dev



  # Optional: Load Testing (commented out by default)
  # locust-master:
  #   image: locustio/locust
  #   ports:
  #     - "8089:8089"
  #     - "5557:5557"
  #   volumes:
  #     - ./core/locust:/mnt/locust
  #   command: -f /mnt/locust/locustfile.py --master -H http://backend:8000/
  #
  # locust-worker:
  #   image: locustio/locust
  #   volumes:
  #     - ./core/locust:/mnt/locust
  #   command: -f /mnt/locust/locustfile.py --worker --master-host locust-master
  #   depends_on:
  #     - locust-master

volumes:
  # redis_data:
  #   driver: local

  postgres_data:
    driver: local
  
  # smtp_data:
  #   driver: local